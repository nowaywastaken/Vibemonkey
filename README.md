# VibeMokey 🐵

> 智能油猴脚本生成 Chrome 扩展 - 基于 DeepSeek V3.2 与 Mem0 记忆系统

## ✨ 特性

- **智能生成**: 使用 DeepSeek V3.2 根据自然语言需求生成油猴脚本
- **DOM 分析**: Prune4Web 策略自动剪枝和语义提取
- **记忆系统**: Mem0 三级记忆架构记住用户偏好和网站知识
- **脚本仓库**: 集成 GreasyFork 搜索现有脚本
- **自愈机制**: 自动检测运行时错误并提供修复建议

## 🚀 快速开始

```bash
# 安装依赖
npm install

# 开发模式 (自动打开 Chrome 并加载扩展)
npm run dev

# 构建生产版本
npm run build
```

## ⚙️ 配置

首次使用需要配置 API Key：

1. 点击扩展图标打开弹出窗口
2. 点击 ⚙️ 设置按钮
3. 输入 [OpenRouter](https://openrouter.ai/) API Key
4. (可选) 输入 [Mem0](https://mem0.ai/) API Key

## 📦 项目结构

```
vibemonkey/
├── entrypoints/           # 扩展入口点
│   ├── background.ts      # Service Worker
│   ├── content.ts         # Content Script
│   └── popup/             # 弹出窗口 UI
├── lib/                   # 核心库
│   ├── agent/             # DeepSeek API + 工具定义
│   ├── memory/            # Mem0 记忆系统
│   ├── dom/               # DOM 剪枝和 Markdown 转换
│   ├── script/            # 脚本生成和仓库集成
│   └── feedback/          # 自愈机制
└── wxt.config.ts          # WXT 配置
```

## 📄 License

MIT
